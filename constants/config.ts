export const LINE_COUNT = 5;
export const LINE_WIDTH = 2;
export const LINES = new Array(LINE_COUNT).fill(null).map((_, i) => i);

export const START_X_WITH_CLEF = 60;
export const START_X_WITHOUT_CLEF = 15;
export const noteSpacing = 30;
export const lineSpacing = 12;